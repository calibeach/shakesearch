import styled from "styled-components"

const StyledListItem = styled.li`
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
  background-color: rgba(216, 204, 191, .4);
  margin: 10px;
  list-style-type: none;
  opacity: 0.8;
  box-shadow: 0 0 10px 5px rgba(216, 204, 191, .4);
`

export { StyledListItem }