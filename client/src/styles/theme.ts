interface DefaultTheme {
    background: string,
    font: {
        family: string
    }
}

const theme: DefaultTheme = {
    background: "#FFFFFF",
    font: {
        family: "Roboto, sans-serif"
    }
}

export { theme }